// functions created for calculating the area and the perimenter .
area = (a) => (a*a),
perimeter = (a) => (4*a) 

// exporting the modules.
module.exports.area = area;
module.exports.perimeter = perimeter;